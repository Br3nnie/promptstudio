ANTHROPIC_API_KEY=your_api_key_here
PORT=3000

# Vercel KV — add these after creating a KV database in the Vercel dashboard
# Storage → KV → Create → Connect to project → copy vars below
KV_REST_API_URL=
KV_REST_API_TOKEN=
